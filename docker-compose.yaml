services:
  oteltester:
    environment:
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      OPENAI_MODEL: ${OPENAI_MODEL}
    build:
      context: .
      dockerfile: Dockerfile
    image: oteltester:latest
    volumes:
      - ./data:/app/data:ro
    ports:
      - "3000:3000"
      - "3001:3001"